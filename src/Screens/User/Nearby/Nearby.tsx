import { View, Text } from "react-native";
import React from "react";
import { NearbyProps } from "./types";

const Nearby = ({ navigation }: NearbyProps) => {
	return (
		<View>
			<Text>Nearby</Text>
		</View>
	);
};

export default Nearby;
