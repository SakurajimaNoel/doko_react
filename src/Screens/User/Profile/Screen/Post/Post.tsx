import { View, Text } from "react-native";
import React from "react";

const Post = () => {
	return (
		<View>
			<Text style={{ color: "black" }}>Create new Post</Text>
		</View>
	);
};

export default Post;
