part of 'image_cropper_helper.dart';

const CropAspectRatio _profileRatio = CropAspectRatio(
    ratioX: Constants.profileWidth, ratioY: Constants.profileHeight);

const CropAspectRatio _contentRatio = CropAspectRatio(
    ratioX: Constants.contentWidth, ratioY: Constants.contentHeight);

const CropAspectRatio _commentRatio = CropAspectRatio(
    ratioX: Constants.commentWidth, ratioY: Constants.commentHeight);
